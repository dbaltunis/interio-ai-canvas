// Email setup status card - hidden as email works out of the box
// Email service is included and active by default (500/month via Resend)
// This component is only imported in SendGridIntegrationTab for context

export const EmailSetupStatusCard = () => {
  // Email works out of the box - no setup required
  return null;
};